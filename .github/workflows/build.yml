name: build

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  modern-26-1:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'microsoft'
      - name: make gradle wrapper executable
        run: chmod +x ./gradlew
      - name: build
        run: ./gradlew -p modern-26.1 build
      - name: capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ravengard-plus-mc26.1-26.1.2
          path: |
            modern-26.1/build/libs/ravengard-plus-*.jar
            !modern-26.1/build/libs/*-sources.jar

  modern:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'microsoft'
      - name: make gradle wrapper executable
        run: chmod +x ./gradlew
      - name: build
        run: ./gradlew build
      - name: capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ravengard-plus-mc26.2
          path: |
            build/libs/ravengard-plus-*.jar
            !build/libs/*-sources.jar

  legacy:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'microsoft'
      - name: make gradle wrapper executable
        run: chmod +x ./gradlew
      - name: build
        run: ./gradlew -p legacy build
      - name: capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ravengard-plus-mc1.21.5-1.21.8
          path: |
            legacy/build/libs/ravengard-plus-*.jar
            !legacy/build/libs/*-sources.jar

  legacy-1-21-9:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'microsoft'
      - name: make gradle wrapper executable
        run: chmod +x ./gradlew
      - name: build
        run: ./gradlew -p legacy-1.21.9 build
      - name: capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ravengard-plus-mc1.21.9-1.21.10
          path: |
            legacy-1.21.9/build/libs/ravengard-plus-*.jar
            !legacy-1.21.9/build/libs/*-sources.jar

  legacy-1-21-11:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
      - name: validate gradle wrapper
        uses: gradle/actions/wrapper-validation@v6
      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'microsoft'
      - name: make gradle wrapper executable
        run: chmod +x ./gradlew
      - name: build
        run: ./gradlew -p legacy-1.21.11 build
      - name: capture build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ravengard-plus-mc1.21.11
          path: |
            legacy-1.21.11/build/libs/ravengard-plus-*.jar
            !legacy-1.21.11/build/libs/*-sources.jar

  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [modern-26-1, modern, legacy, legacy-1-21-9, legacy-1-21-11]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: checkout repository
        uses: actions/checkout@v6
      - name: download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: release
          merge-multiple: true
      - name: publish version release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="$(sed -n 's/^mod_version=//p' gradle.properties)"
          modern_26_1_version="$(sed -n 's/^mod_version=//p' modern-26.1/gradle.properties)"
          legacy_version="$(sed -n 's/^mod_version=//p' legacy/gradle.properties)"
          legacy_1_21_9_version="$(sed -n 's/^mod_version=//p' legacy-1.21.9/gradle.properties)"
          legacy_1_21_11_version="$(sed -n 's/^mod_version=//p' legacy-1.21.11/gradle.properties)"
          test -n "$version"
          test "$version" = "$modern_26_1_version"
          test "$version" = "$legacy_version"
          test "$version" = "$legacy_1_21_9_version"
          test "$version" = "$legacy_1_21_11_version"
          tag="v$version"

          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $tag already exists."
            exit 0
          fi

          gh release create "$tag" release/*.jar \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "Ravengard Plus $version" \
            --generate-notes
